<!-- <p id="notice"><%#= notice %></p> -->

<h1>Listing Communities</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @communities.each do |community| %>
      <tr>
        <td><%= link_to community.name, community_path(community) %></td>
        <% if user_signed_in? %>
          <td><%= link_to 'Show', community_path(community) %></td>
          <td><%= link_to 'Edit', edit_community_path(community) %></td>
          <td><%= link_to 'Destroy', community_path(community), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Community', new_community_path if user_signed_in? %>
