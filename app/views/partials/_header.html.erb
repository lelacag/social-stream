<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%# if user_signed_in? %>
        <%#= link_to "Set Phrase", user_dashboards_path(current_user), class: "navbar-brand" %>
      <%# else %>
      <% if Rails.env == "production" %>
        <%= link_to image_tag("logo.jpg"), "http://dev.setphrase.com/", class: "navbar-brand" %>
      <% else %>
        <%= link_to image_tag("logo.jpg"), "http://dev.setphrase.dev/", class: "navbar-brand" %>
      <% end %>
      <%# end %>
    </div>
    <div id="navbar" class="collapse navbar-collapse" >
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <div class="row">
            <div class="col-md-6 nopadding">
              <figure class="triangle ml-20">
                <%= image_tag current_user.avatar.url(:thumb), size: "50x50" %>
              </figure>
            </div>
            <div class="col-md-6 nopadding">
              <li class="dropdown mt-30 pull-right">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                  <%= current_user.username || current_user.name %><!--  <span class="caret"></span> -->
                </a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to 'Dashboard', user_dashboard_path(current_user.username || current_user.id) %></li>
                  <li><%= link_to 'Settings', edit_user_registration_path %></li>
                  <li class="divider"></li>
                  <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete  %></li>
                </ul>
              </li>
            </div>
          </div>
        <% else %>
          <li><%= link_to 'Login', new_user_session_path %></li>
          <li><%= link_to 'Sign up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
